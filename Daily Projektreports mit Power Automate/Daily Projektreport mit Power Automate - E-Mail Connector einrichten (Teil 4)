Diese Datei dient als begleitendes Material zu meinem YouTube-Video und soll als Hilfestellung für die darin behandelten Themen dienen. 
Sie enthält alle Code-Beispiele, die im Video verwendet wurden, und ermöglicht es dir, die Schritte nachzuvollziehen und direkt anzuwenden.

1) Text des dynamischen Betreffs eintragen:
Projektreport vom [Datum]

2) Text des dynamischen Bodys
Hallo, anbei erhälst du die aktuelle Projektliste vom [Datum].

3) Code die Hintergrundfarbe:
if(lessOrEquals(formatDateTime(item()?['ProjektEndTermin'], 'yyyy-MM-dd'), formatDateTime(utcNow(), 'yyyy-MM-dd')), 'background-color: #CD2626;', '')

4) Code für die Schriftfarbe:
if(lessOrEquals(formatDateTime(item()?['ProjektEndTermin'], 'yyyy-MM-dd'), formatDateTime(utcNow(), 'yyyy-MM-dd')), 'color: #000000;', '')
